fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
python-dotenv==1.0.1

# 임베딩 (ONNX Runtime 기반, Ollama 의존성 제거 — ADR-011)
# Dense : onnxruntime + tokenizers(Rust, PyTorch 불필요) 로 BAAI/bge-m3(1024dim) ONNX 직접 호출
#         transformers는 huggingface_hub(snapshot_download)에 필요하여 유지.
#         AutoTokenizer 대신 tokenizers.Tokenizer.from_file() 사용 (4.52+ 호환성 문제 회피)
#         (FastEmbed는 bge-m3 미지원, sentence-transformers는 external-data 로딩 버그로 회피)
# Sparse: fastembed SparseTextEmbedding(Qdrant/bm25) — BM25 IDF 가중치
onnxruntime>=1.17.0
tokenizers>=0.19.0
transformers>=4.40.0
sentencepiece>=0.2.0
fastembed>=0.8.0
numpy>=1.24.0

# 문서 청킹 (knowledge ingestion 파이프라인용)
python-docx>=1.1.0
python-pptx>=0.6.23
pdfplumber>=0.11.0
pypdf>=4.0.0             # PDF 페이지별 이미지 추출 (DCT/Flate/CCITT 인코딩 정규화 → PIL 호환 bytes)
openpyxl>=3.1.0
beautifulsoup4>=4.12.0   # Confluence HTML 파싱

# 이미지 OCR (Tesseract 기반)
pytesseract>=0.3.13
Pillow>=10.0.0
