# AOMS — Phase 0: 사전 준비

> **이전 단계**: 없음  
> **다음 단계**: [Phase 1-1 인프라 설정 파일 작성](./phase1-config.md)  
> **관련 문서**: [아키텍처 설계서](../architecture-design.md)

---

## 폐쇄망 사전 준비 (macOS에서 수행)

> 모든 설치 파일과 Docker 이미지를 인터넷 연결된 Mac에서 먼저 준비한 뒤, 모니터링 서버 및 대상 서버로 전송합니다.

### 1. Docker 이미지 Pull & Save (Mac)

```bash
# ⚠️ 반드시 --platform linux/amd64 명시
# Mac(Apple Silicon)은 arm64이므로 누락 시 서버에서 실행 불가
docker pull --platform linux/amd64 prom/prometheus:v3.10.0
docker pull --platform linux/amd64 prom/alertmanager:v0.31.1
docker pull --platform linux/amd64 grafana/loki:3.6.7
docker pull --platform linux/amd64 grafana/grafana:12.4.0
docker pull --platform linux/amd64 postgres:16-alpine

# 아키텍처 확인 (저장 전 반드시 검증)
docker inspect prom/prometheus:v3.10.0 | grep Architecture
# → "Architecture": "amd64"  ← 반드시 amd64 확인

# 이미지를 tar 파일로 저장
mkdir -p ~/aoms-offline/docker-images

docker save prom/prometheus:v3.10.0       | gzip > ~/aoms-offline/docker-images/prometheus.tar.gz
docker save prom/alertmanager:v0.31.1     | gzip > ~/aoms-offline/docker-images/alertmanager.tar.gz
docker save grafana/loki:3.6.7            | gzip > ~/aoms-offline/docker-images/loki.tar.gz
docker save grafana/grafana:12.4.0        | gzip > ~/aoms-offline/docker-images/grafana.tar.gz
docker save postgres:16-alpine            | gzip > ~/aoms-offline/docker-images/postgres.tar.gz

# ⚠️ 버전별 주요 변경사항 / 상호 호환성 메모
#
# [Prometheus v3.10.0]
#   - v2.x → v3.x 주요 변경: 기존 설정 파일은 대부분 호환
#   - deprecated된 storage.tsdb.* 일부 플래그 제거됨
#   - web.yml Basic Auth 구조는 동일하게 사용 가능
#   - Alertmanager v0.27+ API v1 완전 제거 → v0.31.1 함께 사용 필수
#
# [Alertmanager v0.31.1]
#   - Prometheus v3와 완전 호환
#   - UTF-8 strict 모드 기본 지원
#   - Teams webhook 방식 동일하게 사용 가능
#
# [Grafana 12.4.0]
#   - 이미지명 변경: grafana/grafana-oss → grafana/grafana (v12.4.0부터)
#   - Prometheus v3, Alertmanager v0.31.1, Loki v3.6.7 모두 데이터소스 지원
#   - UID 472 동일 적용
#
# [Loki v3.6.7]
#   - Grafana 12.x와 완전 호환
#   - Promtail v3.6.7과 동일 버전 사용 (버전 일치 권장)
#   - BoltDB 스토리지 제거됨 → TSDB 기본 사용

# 커스텀 서비스 이미지 빌드 후 저장 (소스 작성 완료 후)
# docker build -t aoms-admin-api:1.0 ./services/admin-api
# docker build -t aoms-log-analyzer:1.0 ./services/log-analyzer
# docker save aoms-admin-api:1.0     | gzip > ~/aoms-offline/docker-images/admin-api.tar.gz
# docker save aoms-log-analyzer:1.0  | gzip > ~/aoms-offline/docker-images/log-analyzer.tar.gz
```

### 2. 에이전트 바이너리 다운로드 (Mac)

```bash
mkdir -p ~/aoms-offline/agents/{linux,windows}

# ---- Linux (RedHat 8.9, x86_64) ----
# ⚠️ darwin(Mac)용이 아닌 linux-amd64 버전을 받아야 함
cd ~/aoms-offline/agents/linux

# node_exporter — linux-amd64 명시
curl -LO https://github.com/prometheus/node_exporter/releases/download/v1.10.2/node_exporter-1.10.2.linux-amd64.tar.gz

# promtail — linux-amd64 명시 (Loki와 동일 버전 권장)
curl -LO https://github.com/grafana/loki/releases/download/v3.6.7/promtail-linux-amd64.zip

# jmx_exporter (Java 에이전트)
curl -LO https://repo1.maven.org/maven2/io/prometheus/jmx/jmx_prometheus_javaagent/0.20.0/jmx_prometheus_javaagent-0.20.0.jar

# nginx-prometheus-exporter
curl -LO https://github.com/nginx/nginx-prometheus-exporter/releases/download/v1.1.0/nginx-prometheus-exporter_1.1.0_linux_amd64.tar.gz

# oracledb_exporter
curl -LO https://github.com/iamseth/oracledb_exporter/releases/download/0.5.0/oracledb_exporter.0.5.0-ora18.5.linux-amd64.tar.gz

# postgres_exporter
curl -LO https://github.com/prometheus-community/postgres_exporter/releases/download/v0.15.0/postgres_exporter-0.15.0.linux-amd64.tar.gz

# Trivy — Linux RPM (보안 스캔용)
curl -LO https://github.com/aquasecurity/trivy/releases/download/v0.69.3/trivy_0.69.3_Linux-64bit.rpm

# 파일명 확인 (linux, amd64, x86_64 포함 여부 검증)
ls -lh .
# node_exporter-1.10.2.linux-amd64.tar.gz  ← linux-amd64 확인
# promtail-linux-amd64.zip                ← linux-amd64 확인

# ---- Windows (x86_64) ----
cd ~/aoms-offline/agents/windows

# windows_exporter
curl -LO https://github.com/prometheus-community/windows_exporter/releases/download/v0.25.1/windows_exporter-0.25.1-amd64.msi

# promtail Windows (Loki와 동일 버전)
curl -LO https://github.com/grafana/loki/releases/download/v3.6.7/promtail-windows-amd64.exe.zip

# jmx_exporter (동일 jar 파일 사용)
cp ../linux/jmx_prometheus_javaagent-0.20.0.jar .

# sql_exporter (MSSQL)
curl -LO https://github.com/burningalchemist/sql_exporter/releases/download/0.14.1/sql_exporter-0.14.1.windows-amd64.zip
```

### 3. Python 패키지 오프라인 다운로드 (Mac)

```bash
# ⚠️ 가상환경에서 RHEL 8.9(x86_64) 타겟으로 wheel 파일 다운로드
mkdir -p ~/aoms-offline/pip-packages

python3 -m venv /tmp/venv-download
source /tmp/venv-download/bin/activate

pip download requests python-dateutil bcrypt flask \
  --platform manylinux2014_x86_64 \
  --python-version 3.11 \
  --only-binary=:all: \
  -d ~/aoms-offline/pip-packages/

deactivate
rm -rf /tmp/venv-download
```

### 4. Trivy 오프라인 DB 다운로드 (Mac)

```bash
# Trivy 보안 스캔용 취약점 DB 사전 다운로드
mkdir -p ~/aoms-offline/trivy-db

# Docker 이미지 사전 스캔 (반입 전 검증)
trivy image --severity CRITICAL,HIGH prom/prometheus:v3.10.0
trivy image --severity CRITICAL,HIGH grafana/grafana:12.4.0
trivy image --severity CRITICAL,HIGH grafana/loki:3.6.7
trivy image --severity CRITICAL,HIGH prom/alertmanager:v0.31.1
# → Critical 0건 확인 후 tar 저장 진행

# Trivy 오프라인 DB 캐시 복사
cp -r ~/.cache/trivy/ ~/aoms-offline/trivy-db/
```

### 5. 설치 스크립트 및 설정 파일 포함 패키지 생성 (Mac)

```bash
# aoms 프로젝트 전체를 압축
cd ~
tar czf aoms-offline/aoms-project.tar.gz workspace/ai/aoms/

# 전송용 최종 패키지 확인
ls -lh ~/aoms-offline/
# docker-images/  agents/  pip-packages/  trivy-db/  aoms-project.tar.gz
```

### 6. 설정 파일 줄바꿈 변환 및 검증 (Mac)

```bash
# ⚠️ Mac에서 작성한 파일은 CRLF일 수 있음 — Linux 서버는 LF만 인식

# VS Code 사용 시: 우측 하단 "CRLF" 클릭 → "LF" 변경 후 저장

# 터미널에서 일괄 확인 및 변환
find ~/aoms-offline/ -type f \( -name "*.yml" -o -name "*.yaml" -o -name "*.py" -o -name "*.sh" -o -name "*.sql" \) \
  -exec sed -i '' 's/\r//' {} \;

# 변환 확인
file ~/aoms-offline/agents/linux/*.sh
# → ASCII text  ← 정상 (LF)
# → ASCII text, CRLF  ← 변환 필요 (위 명령 재실행)
```

### 7. 모니터링 서버로 파일 전송 (Mac → RedHat 8.9)

```bash
# ⚠️ 파일 전송 전 체크섬 생성 (파일 깨짐 방지)
cd ~/aoms-offline
md5 docker-images/prometheus.tar.gz > checksums.md5
md5 docker-images/alertmanager.tar.gz >> checksums.md5
md5 docker-images/loki.tar.gz >> checksums.md5
md5 docker-images/grafana.tar.gz >> checksums.md5
md5 agents/linux/node_exporter-1.10.2.linux-amd64.tar.gz >> checksums.md5

# SCP로 전송 (또는 USB 매체/내부 파일서버 활용)
scp -r ~/aoms-offline/ user@<monitoring-server-ip>:/opt/

# USB 사용 시 (exFAT 포맷 필수 — FAT32는 4GB 초과 파일 불가)
# cp -r ~/aoms-offline/ /Volumes/USB이름/

# 전송 완료 확인
ssh user@<monitoring-server-ip> "ls -lh /opt/aoms-offline/"

# 서버에서 체크섬 검증 (파일 무결성 확인)
# md5sum -c checksums.md5
# → 모든 파일 OK 확인 후 설치 진행
```

---

## 이중화 시스템 라벨링 규칙

> 이중화 구성(WAS 1번기/2번기 등)에서 **논리 시스템명**과 **서버 인스턴스**를 분리하여 관리합니다.

### 라벨 설계 원칙


| 라벨              | 설명                           | 예시                             |
| --------------- | ---------------------------- | ------------------------------ |
| `system_name`   | **논리 시스템명** — 대시보드/알림 라우팅 기준 | `customer-experience`          |
| `instance_role` | **서버 역할 및 번호** — 서버 구분       | `was1`, `was2`, `web1`, `web2` |
| `host`          | **실제 호스트명 또는 IP**            | `192.168.1.10`                 |


### 적용 예시 (고객경험 시스템)

```yaml
# Prometheus scrape_configs 예시
- job_name: 'customer-experience'
  static_configs:
    - targets: ['192.168.1.10:9100']
      labels:
        system_name: 'customer-experience'   # 논리 시스템명 (통합 대시보드/알림 기준)
        instance_role: 'was1'                # 1번기
        host: 'cx-was01'

    - targets: ['192.168.1.11:9100']
      labels:
        system_name: 'customer-experience'   # 동일 system_name
        instance_role: 'was2'                # 2번기
        host: 'cx-was02'

    - targets: ['192.168.1.12:9100']
      labels:
        system_name: 'customer-experience'
        instance_role: 'web1'
        host: 'cx-web01'

    - targets: ['192.168.1.13:9100']
      labels:
        system_name: 'customer-experience'
        instance_role: 'web2'
        host: 'cx-web02'
```

### 이중화 구성 PromQL 예시

```promql
# 특정 시스템 전체 서버의 CPU 평균
avg by(instance_role) (
  (1 - rate(node_cpu_seconds_total{mode="idle", system_name="customer-experience"}[5m])) * 100
)

# 알림 Rule: system_name 단위로 any 서버가 임계값 초과 시 발생
# → 알림 라벨에 instance_role이 포함되어 어느 서버인지 특정 가능
(1 - avg by(instance, system_name, instance_role) (
  rate(node_cpu_seconds_total{mode="idle"}[5m])
)) * 100 > 80
```

### Promtail 라벨 설정 (이중화 예시)

```yaml
# cx-was01 서버의 promtail.yml (방법3+4 조합 적용)
scrape_configs:
  - job_name: app-logs
    static_configs:
      - targets: [localhost]
        labels:
          system_name: 'customer-experience'  # 논리 시스템명
          instance_role: 'was1'               # 서버 구분
          host: 'cx-was01'
          log_type: 'application'
          __path__: /app/logs/*.log
    pipeline_stages:
      - multiline:
          firstline: '^(\d{4}[-/\.]\d{2}[-/\.]\d{2}|...)'
          max_wait_time: 3s
          max_lines: 200
      - drop:
          expression: '^\s*$'           # 빈줄 제거
      - drop:
          expression: '(?i)^(?!.*(error|warn|fatal|exception|fail))'  # 에러만 전송
      - regex:
          expression: '(?i)(?P<level>ERROR|WARN(?:ING)?|FATAL|CRITICAL|INFO|DEBUG)'
      - labels:
          level:
```

### Admin DB 시스템 등록 예시

```json
// 이중화 시스템은 system_name 하나로 등록,
// 서버 목록은 별도 hosts 필드로 관리
{
  "system_name": "customer-experience",
  "display_name": "고객경험 시스템",
  "hosts": [
    {"role": "was1", "ip": "192.168.1.10", "os": "linux"},
    {"role": "was2", "ip": "192.168.1.11", "os": "linux"},
    {"role": "web1", "ip": "192.168.1.12", "os": "linux"},
    {"role": "web2", "ip": "192.168.1.13", "os": "linux"}
  ]
}
```

---

## 8. Server B AI/데이터 서버 사전 준비 (Phase 4b 도입 시)

> Server B (Qdrant + Ollama) 구축 전 Mac에서 사전 준비합니다.

### 8.1 Ollama Docker 이미지 + bge-m3 모델 (Mac)

```bash
# Ollama Docker 이미지 저장
docker pull --platform linux/amd64 ollama/ollama:latest
docker save ollama/ollama:latest | gzip > ~/aoms-offline/docker-images/ollama-latest.tar.gz
# 약 1.5GB

# Mac에 Ollama 설치 후 bge-m3 모델 사전 다운로드
brew install ollama
ollama serve &

# 임베딩 모델 다운로드 (1.4GB)
ollama pull bge-m3
# ⚠️ blobs/ + manifests/ 두 디렉토리 모두 포함 필수
tar czf ~/aoms-offline/ollama-models.tar.gz ~/.ollama/models/

# (선택) fallback LLM: qwen2.5:7b (4.7GB)
# 내부 LLM API 장애 시 백업 전용 — 2코어에서 응답 30~60초
# ollama pull qwen2.5:7b
# tar czf ~/aoms-offline/ollama-models-qwen.tar.gz ~/.ollama/models/
```

### 8.2 Qdrant Docker 이미지 (Mac)

```bash
docker pull --platform linux/amd64 qdrant/qdrant:v1.9.0
docker save qdrant/qdrant:v1.9.0 | gzip > ~/aoms-offline/docker-images/qdrant-v1.9.0.tar.gz
# 약 200MB
```

### 8.3 n8n Docker 이미지 (Mac, Server A용)

```bash
docker pull --platform linux/amd64 n8nio/n8n:1.44.0
docker save n8nio/n8n:1.44.0 | gzip > ~/aoms-offline/docker-images/n8n-1.44.0.tar.gz
# 약 400MB
```

### 8.4 Server B로 파일 전송

```bash
# Server B로 추가 파일 전송
scp ~/aoms-offline/docker-images/ollama-latest.tar.gz user@<server-b-ip>:/opt/aoms-offline/docker-images/
scp ~/aoms-offline/docker-images/qdrant-v1.9.0.tar.gz  user@<server-b-ip>:/opt/aoms-offline/docker-images/
scp ~/aoms-offline/ollama-models.tar.gz                 user@<server-b-ip>:/opt/aoms-offline/

# Server A로 n8n 이미지 전송
scp ~/aoms-offline/docker-images/n8n-1.44.0.tar.gz user@<server-a-ip>:/opt/aoms-offline/docker-images/
```

### 8.5 사전 준비 체크리스트 (추가분)

| 항목 | 파일명 | 크기 | 전송처 |
|------|--------|------|--------|
| Ollama 이미지 | ollama-latest.tar.gz | ~1.5GB | Server B |
| bge-m3 모델 | ollama-models.tar.gz | ~1.4GB | Server B |
| Qdrant 이미지 | qdrant-v1.9.0.tar.gz | ~200MB | Server B |
| n8n 이미지 | n8n-1.44.0.tar.gz | ~400MB | Server A |

